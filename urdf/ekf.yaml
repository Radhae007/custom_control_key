/**:
    ros__parameters:
        frequency: 20.0
        sensor_timeout: 0.3
        two_d_mode: true
        publish_tf: true

        transform_time_offset: 0.1
        transform_timeout: 0.1
        print_diagnostics: true
        debug: false

        map_frame: map
        odom_frame: odom
        base_link_frame: base_footprint
        world_frame: odom

        odom0: /odom1
        odom0_config: [false, false, false,
                      false, false, true,
                      true, true, false,
                      false, false, true,
                      false, false, false]

        imu0: /imu/data
        imu0_config: [false, false, false,
                      false, false, true,
                      false, false, false,
                      false, false, true,
                      false, false, false]

# STEP1 - SET ALL THE Z VALUES TO FALSE (done with 2d_mode=true )
# STEP2 - SET VELOCITY OF ENC TRUE , POSITION FALSE -- POSISION CALCULATED FROM VELOCITY IN CASE OF ENCODER , THUS SAME ERROR COMES HERE ALSO .
# STEP3 
